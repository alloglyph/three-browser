**three browser** gives a framework with which you can interact with an object you have created in
[three.js][0]. By default, a `rotationGroup` exported by `object.js` will be used as the model
object.
